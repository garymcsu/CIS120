/*$$$$$$$$\         $$$$$$\   $$$$$$\   $$$$$$\ $$$$$$\         
\__$$  __|        $$  __$$ \ $$  __$$\ $$  __$$\ _$$  _|        
   $$ |  $$$$$$\  $$ /  $$ | $$ /  \__|$$ /  \__ |$$ |  $$$$$$$\ 
   $$ | $$  __$$\ $$$$$$$$ | $$$$\     $$$$\      $$ | $$  _____|
   $$ | $$ |  \__|$$  __$$ | $$  _|    $$  _|     $$ | $$ /      
   $$ | $$ |      $$ |  $$ | $$ |      $$ |       $$ | $$ |      
   $$ | $$ |      $$ |  $$ | $$ |      $$ |     $$$$$$\ $$$$$$$\ 
   \__| \__|      \__|  \__| \__|      \__|     \______\ _______*/
                  //  [https://bit.ly/3mkfHUh] //                                     
                                                             
                                                             

// second set of lights
int red = 10;            // pretty much done, some delays are short so it is easier to test.
int yellow = 11;
int green = 12;
int blue = 13;
const int bbuttonPin =7;

// second set of lights
int rred = 2;
int yyellow = 3;
int ggreen = 4;
int bblue = 5;
const int buttonPin = 9;

//------------
void setup() {
  pinMode(red, OUTPUT);
  pinMode(yellow, OUTPUT);
  pinMode(green, OUTPUT);
  pinMode(blue, OUTPUT);
  pinMode(buttonOne, INPUT);
  Serial.begin(115200);
  buttonOneState = digitalRead(buttonOne);
}

void loop(){
Serial.println(buttonOneState);
buttonOneState = digitalRead(buttonOne);
  changeLights();
}

void changeLights(){
  if (buttonOneState == 0) {
    // green off, yellow on for 3 seconds
    digitalWrite(green, LOW);
    digitalWrite(yellow, HIGH);
    delay(3000);
    // turn off yellow, then turn red on for 5 seconds
    digitalWrite(yellow, LOW);
    digitalWrite(red, HIGH);
    delay(5000);
    // turn off red, then turn on green
    digitalWrite(red, LOW);
    digitalWrite(green, HIGH);
    delay(3000);
    }
 else {
  if (buttonOneState == 1) {
    // green off, yellow on for 3 seconds
    digitalWrite(green, LOW);
    digitalWrite(yellow, HIGH);
    delay(2800);
    // turn off yellow, then turn red on for 5 seconds
    digitalWrite(yellow, LOW);
    digitalWrite(red, HIGH);
    delay(6000);
    // turn off red and yellow, then turn on blue
    digitalWrite(yellow, LOW);
    digitalWrite(red, LOW);
    digitalWrite(blue, HIGH);
    delay(3000);
    // green on addition to blue, and turn blue off after 2-seconds leaving green on for 3s
    digitalWrite(green, HIGH);
    delay(2000);
    digitalWrite(blue, LOW);
    delay(3000);
    }
}
}